# AstroWay WP plugin: паритет с RoxyAPI и выход из iframe

Дата: 2026-07-28. Ревизия 3 (второй cross-review: 1 blocker, 3 major, 2 minor, все подтверждены).

## Старт сессии (обновлено 2026-07-31)

**Где мы.** Плагин перестал быть обёрткой над iframe. Server-side отдаются семь виджетов: дневной,
недельный и месячный гороскоп, фаза Луны, Таро, планета дня, натальная карта. Закрыты фаза 0
(релиз 0.10.7 опубликован и принят, `wp plugin check` 0 ERROR против прежних 69), фаза 1 целиком,
3.2 и 3.5. Локально в staging накоплено четыре коммита сверх 0.10.7, в очередь релиза они ещё
не выложены.

**Что делать дальше, в порядке ценности.**

1. **Собрать релиз 0.11.0** из накопленного: `_unreleased/v0.11.0/MANIFEST.json` плюс
   `RELEASE_NOTES.md`, `depends_on.previous_plugin_version` = `0.10.7`. В списке файлов не забыть
   новые `includes/class-public-data.php`, `includes/class-render.php` и `languages/` после
   `wp astroway i18n sync`: карточки принесли около 45 новых строк (знаки, планеты, аспекты, дома).
   Гейты workflow прогоняются локально скриптом, см. историю: правила манифеста, cadence,
   version guard, полнота сборки, `php -l`.
2. **Фаза 5, витрина wp.org.** Самое дешёвое из оставшегося и ровно то место, где конкурент нас
   обходит. 5.1 требует пересъёмки скриншотов: нынешние сняты при пяти блоках и старой админке.
3. **Фаза 4, генератор покрытия из OpenAPI.** Большая работа, но теперь осмысленная: до
   server-side рендера 152 шорткода в iframe с noindex не дали бы ничего.

**Фазу 2 (форма для посетителя) рекомендую не брать.** Полезна только на установке с ключом, а
поиск города зарегистрирован лишь как `wp_ajax_`, без `nopriv`: публичная точка это отдельная
работа с лимитами, и наивный `nopriv` открывает неаутентифицированный прокси к `app.astroway.info`.

**Чего отсюда сделать нельзя.** Всё, что живёт в api, идёт промтом через Максима:
`docs/handoff-api-2026-07-30.md`, готов к вставке. Там незапушенный коммит `2619e5711e0`, ловушка
`.passthrough()` в `POST /v1/public/chart`, анонимный источник для бодиграфа (3.1), blanket
`noindex` (3.3), CORS (3.4). **В начале сессии напомнить Максиму отправить эти промты.**

## Архитектурное решение: три слоя рендера

Вопрос «iframe или `<astroway-horoscope-card>`» поставлен неверно, это три независимых слоя:

1. **Транспорт**: кто ходит в API, браузер посетителя или PHP нашего сервера.
2. **Место рендера**: отдельный документ в iframe или разметка в DOM страницы.
3. **Слой интерактива**: ничего, custom element, полноценный JS-виджет.

Индексацию, наследование темы, доступность и вёрстку решает **слой 2**. Custom element сам по себе
не решает ничего: если данные тянутся фетчем, для краулера это та же пустота, что и iframe.
Клиентский фетч у нас вдобавок закрыт CORS-whitelist'ом.

Как это сделано у конкурента: PHP кладёт `<roxy-horoscope-card>`, внутрь него fallback-разметку
**и** `<script type="application/json">` с полными данными, компонент апгрейдит уже готовый HTML.
Цена `roxy-ui.js` 439 КБ, лениво подключается только на страницах с виджетом.

**Принятое решение:** копируем слоистость, не копируем цену.

- Слой 2 переезжает на server-side PHP-рендер. Это и есть содержание 1.0.0.
- Тег `<astroway-horoscope-card>` вводится **сразу**, как обёртка серверной разметки, без единой строки JS.
  Разметка внутри работает сама. Имя тега дёшево ввести сейчас и дорого вводить потом:
  апгрейд до интерактива не потребует ни менять шаблоны, ни ломать CSS пользователей второй раз.
- Слой 3 добавляется точечно и позже, только где интерактив нужен: ховер по планетам на колесе,
  аккордеоны интерпретаций, вкладки. У нас сейчас фронтового JS для виджетов ноль байт,
  начинать с чистого листа дешевле, чем тащить чужой бандл.
- iframe не выбрасывается, а понижается до явного запасного режима там, где server-side пока невозможен.

Ломающее изменение разметки делается **один раз и сейчас**: при <10 активных установках
цена почти нулевая, через полгода она станет реальной. Semver при смене разметки требует мажора.

## Релизная стратегия

Замысел «мелкий хотфикс 0.9.3» не выжил при встрече с фактами: файлы, которые трогает фаза 0,
разошлись с опубликованной 0.9.2 на 621 строку, потому что пять версий стояли в очереди
неопубликованными. Манифест копирует файлы целиком, поэтому мелкого diff не получилось бы.

- **0.10.7, выпущено в очередь 2026-07-29.** Фаза 0 плюс всё накопленное: пять embed-блоков,
  фикс `lng`+`tz` для натала и бодиграфа, брендовая сова, фикс verify-key, opt-in disclaimer.
  `previous_plugin_version` = 0.9.2, `Tested up to` = 7.0.
- **Отменены совсем:** `v0.9.3` Planetary hour и `v0.9.4` Fortune Cookie. Оба расширяют виджеты,
  чьих роутов в api не существует. Ставить в очередь заново вместе с работой по api.
- **1.0.0, переезд рендера.** Фаза 1 плюс тег `<astroway-horoscope-card>`.
- Быстрые паритетные победы для 1.0.0 без генератора: в embed-роутере api 15 виджетов
  (kundli, panchang, synastry, transit, transit-timeline, numerology, planet-of-day,
  monthly-forecast, mini-chart, natal), плагин экспонирует 6.

## Какой код мы правим

Три разные сборки, и путать их нельзя, ревизия 1 плана на этом ошиблась:

- **wp.org 0.9.2** это то, что стоит у пользователей. Только `astroway.zip` с каталога является предметом фазы 0.
- **песочница `astroway-plugin.test`: 0.10.6**, сборка GitHub-канала с dev-файлами. В ней есть код, которого нет в каталоге.
- **рабочее дерево репозитория: ветка `main`, HEAD на 5 релизных коммитов позади тега v0.9.2.** Виджета `today_in_sky` в нём физически нет.

Разрыв между каталогом и локальной веткой в несколько версий, поэтому у каждого фикса фазы 0 надо
отдельно определять точку входа в релизный конвейер (см. 0.8).

## Baseline (замерено, источник указан)

| | AstroWay 0.9.2 | RoxyAPI 1.6.6 |
|---|---|---|
| В каталоге с | 2026-05-14 | 2026-05-16 |
| Загрузок | 1113 / 75 дней | 1182 / 73 дня |
| Активных установок | <10 | <10 |
| Отзывов | 0 | 0 |
| Шорткоды / блоки | 6 / 5 | 152 / 144 |
| PHP LOC всего | 9 841 | 30 642 |
| PHP LOC без vendor | ~2 736 | ~30 642 |
| Локали UI | 20 | 0 (только .pot) |
| Tested up to (wp.org API) | 6.9.5 | 7.0.2 |
| Plugin Check, ERROR-классов в составе zip | 8 | 0 |

Замер контента: тестовая страница `/roxy-test/` с 5 их шорткодами и 2 нашими даёт 31 891 символ текста
в DOM, из которых наши шорткоды не дают ничего: весь наш вывод уходит в iframe
с `x-robots-tag: noindex, nofollow`. Страница `/our-only/` с 6 нашими шорткодами: 243 символа,
и это заголовок с обвязкой темы.

Вывод из baseline: разрыв в числе шорткодов (6 против 152) не является причиной отставания,
потому что у обоих плагинов одинаковый результат по активным установкам и отзывам. Причина
отставания в том, что плагин не кладёт контент на страницу и ломается в бесплатном режиме.

## Установленные факты об API (проверены curl 2026-07-28)

- `/v1/embed/*` анонимно отдаёт HTML, лимит 30/час по IP, на всех путях `x-robots-tag: noindex, nofollow`.
- `/v1/public/horoscope/daily`, `/v1/public/moon-phase`, `/v1/public/tarot/daily` анонимно отдают JSON, тот же лимит 30/час по IP.
- `POST /v1/public/chart` **анонимно отдаёт полную натальную карту** (HTTP 200, 33.7 КБ: planets, houses, aspects, antiscia). GET-пути `/v1/public/natal`, `/bodygraph`, `/human-design` отдают 401, но это не значит, что анонимного натала нет. Контракт полей надо выяснить до реализации: тестовый POST с `lat`/`lng`/`tz` вернул карту для координат 0,0, то есть переданные значения были проигнорированы.
- Анонимного источника нет только для бодиграфа и Human Design.
- В `/v1/public/*` есть также `horoscope/weekly`, `horoscope/monthly`, `planet-of-day`.
- Роута `today-in-sky` в api **не существует вовсе** (в `embed.ts` 15 роутов, этого среди них нет). Живой 401 это провал сквозь embed-роутер в общий auth-wall, а не эндпоинт за ключом.
- Без параметра `lang` контент приходит на украинском.
- CORS на `/v1/public/*` работает **по whitelist**: произвольный домен не получает `access-control-allow-origin`. Браузерный fetch с сайта пользователя будет заблокирован.
- Заголовок `X-AstroWay-Site-URL` разрешён в `access-control-allow-headers`, но на учёт лимита не влияет: счётчик уменьшается по IP независимо от него. Учёта по домену на API нет.
- **Ключ не спасает от анонимного лимита на публичных путях.** `publicRateLimitMiddleware` смонтирован на `/api/v1/public` (index.ts:117) и `/api/v1/embed` (index.ts:135), то есть до `app.use('/api/v1', authMiddleware)` (index.ts:174), и считает `req.ip` без всякой проверки ключа. Бакеты `user:`/`key:` работают только на authed-роутах, смонтированных после auth. Server-side вызов на `/v1/public/*` даже с ключом тикает IP-счётчик сервера.
- **Ключ физически не попадает в iframe**: `PublicClient::embed_url()` собирает URL только из whitelisted-параметров виджета. Поэтому у платящих установок iframe жжёт анонимную квоту посетителя.

## Почему у нас iframe (контекст)

Коммит v0.1.0 (2026-05-12): анонимный тариф считается по IP посетителя, поэтому MVP переложил вызов
на браузер через iframe, и только так работает обещание «no account, no key». README плагина
с самого начала описывал гибрид с client-side fetch из `/v1/public/*`, но гибрид не был реализован:
все 6 шорткодов безусловно зовут `embed_iframe()`. Как выяснилось при ревью, client-side fetch
и не мог работать: CORS на `/v1/public/*` закрыт whitelist'ом.

## Фаза 0. Критические дефекты

**Статус: закрыта в коде 2026-07-29, ждёт публикации.** Все правки лежат в staging,
собраны в релиз `v0.10.7` (очередь из пяти версий схлопнута в него, см. «Релизная стратегия»).
Проверено: phpunit 40/40, `wp plugin check` 0 ERROR на симуляции wp.org-сборки, песочница WP 7.0.2.
Отметка `[x]` означает «сделано и проверено локально»; проверка в каталоге возможна только
после публикации 0.10.7.


- [x] **0.0 Синхронизировать рабочее дерево.** Сделано 2026-07-28: rebase на origin/main,
      версия в `astroway-wp-plugin.php` теперь 0.9.2, `today_in_sky` появился в дереве
      (`includes/class-shortcodes.php`, `includes/class-tier.php`). Фазу 0 есть где выполнять.
- [x] **0.1 `[astroway_today_in_sky]` не работает НИ У КОГО, включая платящих.** Роута
      `today-in-sky` в api не существует, а ключ в iframe не попадает в принципе, поэтому
      гейт через `Tier::can()` продал бы ключ, после покупки которого виджет остался бы битым.
      Затронуты 0.9.0-0.9.2 и все 0.10.x. Виджет выпускался тремя релизами подряд и не работал ни разу.
      Решение для 0.9.3 (без правок api): снять шорткод и блок с публикации, зарегистрировав
      депрекейт-заглушку, чтобы у поставивших его страницы не падали.
      Возврат в 1.0.0 вместе с созданием роута в api.
      Верификация: страница с шорткодом не содержит ни iframe с 401, ни пустого блока;
      в api появился роут (для 1.0.0).
- [x] **0.2 Probe гасит виджеты для всех посетителей.** `PublicClient::rate_limit_active()`
      (`includes/class-public-client.php:42-105`) шлёт HEAD с IP сервера, а рендерят браузеры посетителей
      со своими квотами. Один исчерпанный серверный лимит возвращает `''` из всех шорткодов.
      Решение: не подавлять рендер по серверному probe. Внимание: в 0.9.2 опция `astroway_rate_limit_hit_at`
      пишется и **не читается никем**, admin-notice о лимите появляется только с 0.10.6, поэтому
      «оставить probe источником notice» до 0.10.x не работает.
      Верификация: при исчерпанном серверном лимите фронт продолжает рендерить.
- [x] **0.3 Plugin Check: 8 ERROR-классов в составе опубликованного zip.**
      `missing_direct_file_access_protection` (38), `EscapeOutput` (8), `plugin_updater_detected` (5)
      и пять классов I18n (TextDomainMismatch, MissingTranslatorsComment, MissingArgDomain,
      NonSingularStringLiteralText, NonSingularStringLiteralDomain). Исключить PUC недостаточно:
      `plugin_updater_detected` срабатывает и на наш `includes/class-updater.php`
      (`PucFactory::buildUpdateChecker`, `site_transient_update_plugins`), его нужно исключать
      или стабить в wp.org-сборке.
      Верификация: `wp plugin check` на **распакованном wp.org-zip** даёт 0 ERROR. Проверять негде:
      Plugin Check работает только по установленному плагину, а в песочнице стоит 0.10.6.
      Нужен второй WP-инстанс либо временная подмена с возвратом, это часть работы по пункту.
- [x] **0.4 Две мёртвые настройки.** `render_mode` (v0.8.0) и `spend_cap_usd` (v0.8.1) валидируются
      и сохраняются в `class-admin.php:113-120`, рендерятся в `views/admin-settings.php`, и не читаются
      нигде. Пользователь платит вниманием за переключатели, которые ничего не делают.
      Решение: скрыть из UI до реализации, `render_mode` подключить в фазе 1.
      Верификация: в UI не осталось настроек без читателя (grep по каждому ключу опции).
- [x] **0.5 Admin-notices без проверки экрана.** В 0.9.2 это один `maybe_activation_notice`
      (`class-plugin.php:67`), в 0.10.6 к нему добавлены `maybe_review_prompt` и `maybe_rate_limit_notice`
      (`class-plugin.php:72-74`). Ни один не проверяет текущий экран, поэтому лезут на страницы чужих
      плагинов (наблюдалось на `admin.php?page=roxyapi`). Чинить до того, как 0.10.x уйдёт в каталог.
      Верификация: notices отсутствуют на `admin.php?page=roxyapi` и на общих экранах WP.
- [x] **0.6 Tested up to 7.0.** Сейчас 6.9.5 при актуальном WP 7.0.2.
      Верификация: страница каталога показывает 7.0, прогон в песочнице на 7.0.2 без ошибок.
- [x] **0.7 Changelog обещает то, чего нет в коде.** Запись `= 0.5.6 =` в readme.txt 0.9.2 заявляет
      review prompt через 14 дней и one-time admin notice о лимите. В коде 0.9.2 нет ни того, ни другого.
      Это второй сбой того же конвейера: запись `= 0.5.7 =` описывает, как из ZIP выпали три файла
      и каждая загрузка страницы падала с fatal.
      Решение: привести changelog в соответствие с составом сборки и добавить в конвейер проверку
      «заявленное в changelog присутствует в собранном zip».
      Верификация: проверка падает на намеренно расхождении и проходит на текущем релизе.
- [x] **0.8 Определить точку входа фиксов в релизный конвейер.** wp.org на 0.9.2, локально 0.10.6,
      в очереди manifest'ы следующих версий. Нужно решить: хотфикс поверх опубликованной версии или
      прогон через очередь. От этого зависит, когда фиксы 0.1-0.7 увидят реальные пользователи.

- [x] **0.9 Приёмка публикации 0.10.7.** Опубликовано 2026-07-29 23:44 GMT, ревизия SVN 3627762.
      Версия в каталоге 0.10.7. `wp plugin check` на скачанном с wp.org архиве: **0 ERROR**, два
      warning'а (`load_plugin_textdomain` признан лишним для wp.org-хостинга, и расхождение имени
      между readme и заголовком плагина, оно намеренное под выдачу каталога). До фазы 0 было
      69 ERROR. Апдейтера нет ни в trunk, ни в теге 0.10.7, ни в архиве; остался пустой каталог
      `includes/lib/`, косметика. Все десять блоков названы в живом листинге.
      Попутно найдено и починено два дефекта: CI падал на каждом пуше с 28 июля из-за выравнивания
      в `admin-settings.php`, которое сломал я сам при удалении панелей, а `apply-release.yml`
      гоняет `composer lint` как gate, то есть публикация оборвалась бы на шаге проверок. И подписи
      к скриншотам 3 и 4 заявляли пять карточек и пять блоков против десяти в описании: исправлено
      в staging, public и в SVN (trunk плюс тег, иначе wp.org рендерит из stable tag и правка
      не появляется).

## Фаза 1. Server-side рендер лёгких виджетов

Цель: контент в DOM, индексируемый, наследующий тему сайта. API не трогаем.

- [x] 1.1 PHP-клиент к `/v1/public/*` с WP-транзиент кэшем. **Сделано 2026-07-30**, класс
      `PublicData` (`includes/class-public-data.php`), коммит `bef264a`, 22 теста. TTL взят
      не «по умолчанию час», а по природе данных, см. 1.9. Шесть виджетов: `horoscope_daily`,
      `horoscope_weekly`, `horoscope_monthly`, `tarot_daily`, `moon_phase`, `planet_of_day`.
      Список сверен с роутером api, а не с намерением: это ровно те `router.get` в
      `src/routes/public.ts`, что существуют.
      **Формы ответов, снятые живыми запросами 2026-07-30** (нужны для 1.2, повторно не выяснять):
      гороскоп даёт `sign, date, period, horoscope` (строка, у daily около 1260 символов),
      `disclaimer, model, language`; `moon-phase` даёт `date, phase, phaseName, majorPhase,
      illuminationPercent, elongationDeg, ageDays, waxing, moonSign, sunSign`; `planet-of-day` даёт
      `date, weekday, planet, glyph, themes[]`; `tarot/daily` даёт `date, spread{slug,name,cardCount},
      seed, drawn[]{position{index,name,meaning}, card{slug,number,...}}`.
      Отсутствующий знак возвращает null без запроса: подставлять чужой знак хуже, чем не отрисовать.
      Битая дата отбрасывается, api отвечает за сегодня: гасить виджет из-за опечатки в одном
      атрибуте хуже. Вызывающая сторона обязана трактовать null как «переключайся», а не как
      «выведи пустой блок», это 1.5.
- [x] 1.2 Шаблоны рендера. **Сделано 2026-07-30**, класс `Render` (`includes/class-render.php`),
      коммит `644731d`, 16 тестов. Четыре кастомных тега: `astroway-horoscope-card` (daily, weekly,
      monthly), `astroway-moon-card`, `astroway-tarot-card`, `astroway-planet-card`. JS не
      подключается и не нужен: незарегистрированный тег с дефисом это обычный styleable
      HTMLElement. Стили опираются на `currentColor`, а не на свою палитру, поэтому карточка
      выглядит родной и в светлой теме, и в тёмной без настройки с обеих сторон. Проверено
      скриншотами в обеих и на 390px, где список деталей схлопывается в одну колонку.
      Подключены три существующих шорткода: `astroway_daily_horoscope`, `astroway_moon_phase`,
      `astroway_tarot_card`. Шаблоны weekly, monthly и planet-of-day написаны, но своих шорткодов
      пока не имеют, это идёт вместе с 1.3.
      Таро уходит на сервер только для Rider-Waite: публичный роут тянет карты только из этой
      колоды, остальные остаются на iframe, иначе под правильным заголовком приедут карты чужой
      колоды. Прямое и перевёрнутое толкования приходят оба на каждой карте, выбор делает расклад,
      и это закреплено тестом: не та ветка переворачивает смысл, выглядя при этом нормально.
- [x] 1.3 `render_mode` подключён к рендеру в двух рабочих значениях. **Сделано 2026-07-30**,
      коммит `5b4c7de`. Панель вернулась в настройки, на этот раз читаемая: в 0.10.7 её удалили
      именно потому, что значение сохранялось и никем не читалось. Значение `client` убрано из
      принимаемых: CORS на `/v1/public/*` закрыт whitelist'ом, браузерный fetch с произвольного
      домена не пройдёт, вернётся в 3.4. Рендер считает server-side всё, что не равно `iframe`
      явно, иначе оставшийся в базе от старой сборки `client` молча погасил бы все виджеты.
      Заодно появились шорткоды `astroway_weekly_horoscope`, `astroway_monthly_horoscope`,
      `astroway_planet_of_day`: шаблоны для них уже были написаны и лежали недостижимыми.
      Проверено переключением опции: `iframe` даёт три iframe и ноль карточек, `auto` наоборот.
      **Найдено при проверке:** недельный и месячный эндпоинты отдают Markdown (8 жирных фрагментов
      и 13 пунктов списка в одном ответе), рендер печатал звёздочки буквально. Добавлен разбор
      безопасного подмножества: экранирование идёт до вставки тегов, обратный порядок открыл бы
      ровно ту дыру, ради которой всё это. Формат ответа api не декларирует, поэтому разбор на
      нашей стороне, а не надежда на стабильность модели.
- [ ] 1.10 API-сторона: тело недельного и месячного гороскопа начинается с собственного заголовка
      («**Weekly Horoscope for Leo (July 27, 2026)**»), который дублирует заголовок карточки.
      Выкидывать текст эвристикой на нашей стороне неправильно, чинится промптом в api.
- [x] 1.4 Явно передавать `lang`: без него API отдаёт украинский. **Сделано 2026-07-30**: клиент
      всегда подставляет `lang`, резолв перенесён в `Plugin::resolve_lang()`, потому что он читает
      `SUPPORTED_LANGS` и `normalize_locale`, то есть принадлежит `Plugin`, а не `Shortcodes`.
      В `Shortcodes` остался делегат. Ответ api несёт поле `language`, по нему и проверено.
- [x] 1.5 Fallback на iframe при недоступности API, без пустых блоков. **Сделано 2026-07-30.**
      iframe тут правильный запасной вариант именно потому, что ломается иначе: его тянет браузер
      посетителя по квоте посетителя, поэтому сервер с исчерпанной квотой всё равно что-то покажет.
      Тем же путём идёт карточка с пустым содержимым: рамка с заголовком и без текста хуже, чем
      iframe, который она заменила. У кого iframe-аналога нет, посетитель не видит ничего,
      администратор видит короткую заметку.

**Риск фазы снят 2026-07-29 пунктом 3.2** (api 2.112.0 на проде). Серверные вызовы с заголовком
`X-AstroWay-Site-URL` считаются по сайту: 300/час на сайт, стеля 600/час на IP. Браузерный трафик
и iframe не тронуты, у них по-прежнему 30/час на IP посетителя.

Что из этого следует для реализации фазы:

- [x] 1.6 PHP-клиент обязан слать `X-AstroWay-Site-URL: home_url()`. **Сделано 2026-07-30**,
      закреплено тестом: без заголовка server-side рендер сядет на анонимные 30/час и упрётся ровно
      там же, где упирался бы до правки. Проверено живым запросом, ответ приходит с
      `X-RateLimit-Scope: site` и лимитом 300.
- [x] 1.7 Probe отменён, а не починен. **Сделано 2026-07-30.** Каждый рендер и так возвращает
      заголовки квоты, поэтому уведомлению в админке отдельный запрос не нужен: `PublicData`
      записывает последнее показание в опцию, `rate_limit_active()` читает её. Probe остался
      только для сайта, который давно ничего не рендерил, и теперь тоже шлёт заголовок сайта.
      Проверено на живых вызовах: записано `{"limit":300,"remaining":287,"scope":"site"}`, то есть
      уведомление наконец говорит про тот бакет, из которого виджеты действительно тратят.
- [x] 1.8 Негативный кэш на 60 секунд, как у конкурента (`src/Api/Cache.php`, `NEGATIVE_TTL`):
      ошибку (429, 5xx, таймаут, невалидный конверт) класть в транзиент отдельно от успешного
      ответа. **Сделано 2026-07-30.** Без него исчерпанная квота даёт запрос на каждый рендер
      и добивает бакет вместо того, чтобы дать ему восстановиться.
- [x] 1.9 TTL считать от природы данных, а не от «часа по умолчанию». **Сделано 2026-07-30**:
      дневной ответ кэшируется до полуночи UTC, недельный до понедельника, месячный до первого
      числа, потому что api якорит период именно так. Пол в минуту, чтобы смена суток в 23:59:50
      не давала всплеск. Страница с двенадцатью знаками тратит двенадцать вызовов в сутки, а не
      двенадцать в час, и вопрос лимита снимается целиком.

Верификация фазы: весь текст, который отдал api, присутствует в DOM. Порог «не меньше 1500 символов»
из первой редакции плана снят как выдуманный: замер 2026-07-30 показал, что api отдаёт около 1260
символов на дневной гороскоп плюс 186 на дисклеймер, недельный и месячный длиннее. Проверять надо
полноту переноса, а не число. Дальше: Lighthouse без регрессий;
виджет читаем в светлой и тёмной теме; страница выдерживает прогрев кэша без пустых блоков;
скриншоты до и после.

## Фаза 2. Форма для посетителя

Ограничение, которое надо назвать явно: форма предполагает установку **с API-ключом**. Для установок
без ключа каждый посетитель тратит общую анонимную квоту сайта, поэтому там форма либо недоступна,
либо жёстко ограничена.

Независимость от 3.2 достигается **только** если форма ходит на authed-роуты (`POST /v1/chart`
с заголовком `X-Api-Key`), которые смонтированы после `authMiddleware` и считаются в бакете
`user:`/`key:`. Если реализовать её через `/v1/public/*`, даже с ключом, сайт упрётся в 30/час
на IP сервера. Это обязательное требование к реализации, а не деталь.

- [ ] 2.1 Форма для натальной карты и бодиграфа: дата, время, поиск города. Внимание:
      `astroway_atlas_search` зарегистрирован только как `wp_ajax_` (`class-admin.php:25`),
      `wp_ajax_nopriv_` нет ни в одной сборке, поэтому незалогиненному посетителю поиск города
      сейчас недоступен. Нужна отдельная публичная точка со своим лимитом: наивный `nopriv`
      открывает неаутентифицированный прокси к `app.astroway.info/api/atlas/search`.
- [ ] 2.2 Обработка отправки server-side: nonce, лимит по IP посетителя на отправку
      (у конкурента 20/час, `Support/RateLimit.php`), консент-чекбокс.
- [ ] 2.3 Результат на той же странице, ключ не уходит в браузер.
- [ ] 2.4 Поведение без ключа: явное сообщение вместо молчаливой пустоты.

Верификация: заполнение формы даёт карту; повторные отправки ограничиваются; ключа нет в HTML;
на установке без ключа поведение предсказуемо.

## Фаза 3. Тяжёлые виджеты server-side (правки api.astroway.info)

Порядок внутри фазы изменён: узкое место это 3.2, а не 3.1. Натальная карта анонимно уже доступна
через `POST /v1/public/chart`, поэтому её server-side рендер можно вынести в 1.0.0, не дожидаясь фазы 3.

- [x] 3.2 Анонимная квота по домену сайта вместо квоты по IP. **Сделано 2026-07-29**, api 2.112.0,
      коммит `c5469cc31ee`. Заголовок `X-AstroWay-Site-URL` даёт 300/час на сайт плюс стеля
      600/час на IP; без заголовка всё как было, 30/час на IP. Два счётчика, а не один: счётчик
      сайта ловит один сайт с многих адресов (CDN, балансировщик), счётчик IP ловит много имён
      с одного адреса, то есть подделку заголовка. Заголовок это декларация, а не учётные данные,
      поэтому реально ограничивает именно стеля по IP. Отвергнутый запрос квоту не тратит.
      Ответ несёт `X-RateLimit-Scope` со значением `ip` или `site`. Проверено на проде curl'ом.
- [ ] 3.1 Анонимный источник для бодиграфа и Human Design. Для натальной карты **контракт разобран
      2026-07-29**: `POST /v1/public/chart` принимает `date` (YYYY-MM-DD), `time` строго `HH:mm:ss`,
      `timezoneOffset`, `latitude`, `longitude`, `houseSystem`. Прежняя проба возвращала карту для
      координат 0,0 потому, что схема `.passthrough()` и `latitude`/`longitude` имеют default 0:
      поля `lat`/`lng`/`tz` молча проглатываются и не участвуют в расчёте. Ответ под 200 при этом
      выглядит нормальным. Для клиента фазы 1 это значит: имена полей проверять по схеме, а не по
      аналогии с embed-параметрами.
- [ ] 3.3 `noindex` для server-side рендера снимать **не нужно**: фрагмент, полученный нашим сервером,
      уходит посетителю под заголовками страницы сайта. Пункт актуален только для iframe и режима
      `client`, и потребует route-scoped исключения, потому что сейчас `noindex` навешен
      на весь `/api/v1` (index.ts:66-69).
- [ ] 3.4 CORS для `/v1/public/*` под произвольные домены плюс режим `client`, перенесённый из 1.3.
- [x] 3.5 Натал переведён на server-side. **Сделано 2026-07-30**, коммит `d001648`, 13 тестов.
      Карточка держит и колесо, и текст: положения (знак, градус, дом, ретроградность), асцендент,
      MC и мажорные аспекты, около 1150 символов индексируемого текста там, где не было ничего.
      Колесо оставлено намеренно: это картинка, в JSON её нет, а убрать значит забрать визуал
      у всех, у кого шорткод уже стоит. Неудачный ответ откатывается к голому колесу, то есть
      к прежнему поведению.
      Чарт за прошедший момент не меняется, поэтому кэш на месяц, а не до календарной границы,
      и `lang` не отправляется: полезная нагрузка это числа, язык только раздробил бы кэш.
      **Бодиграф остался на iframe**: анонимного источника для него нет, это пункт 3.1, сторона api.
      Две вещи, которые молча ломаются, закреплены тестами: имена полей (`latitude`, `longitude`,
      `timezoneOffset` против `lat`, `lng`, `tz` у embed-роутов, при промахе приходит карта для
      нулевых координат под 200) и поиск дома по куспидам (один интервал из двенадцати всегда
      пересекает 0°).

Верификация: натальная карта в DOM без iframe; проверка индексации тестовой страницы.

## Фаза 4. Покрытие из OpenAPI

- [ ] 4.1 Генератор шорткодов и блоков из `astrology-api-openapi` (по образцу их `src/Generated`).
- [ ] 4.2 Единый рендер по типам ответа, а не рукописный шаблон на эндпоинт.
- [ ] 4.3 Библиотека шорткодов в админке с поиском и копированием.
- [ ] 4.4 Родительский блок с block context для общего знака зодиака у дочерних.

Верификация: число рабочих шорткодов; smoke-тест рендера каждого; размер бандла под контролем.

## Фаза 5. Витрина wp.org

- [ ] 5.1 Скриншоты: сейчас 4 из 5 показывают интерфейс WP (меню, настройки, справочник шорткодов,
      блок в редакторе), продукт показывает только пятый. У них 7 из 8 показывают продукт.
      Вдобавок картинки устарели: сняты при пяти блоках, а плагин отдаёт десять, и админка с тех пор
      изменилась (панели удалены, появилась иконка совы). Подписи 2026-07-29 переписаны так, чтобы
      не называть число, но это заплатка: пересъёмка нужна.
- [ ] 5.5 FAQ и описание говорят «30 запросов в час на IP посетителя». Сегодня это правда, потому
      что плагин отдаёт iframe. Как только появится server-side рендер, текст станет неточным:
      серверные вызовы идут по квоте сайта (300/час). Переписать вместе с фазой 1, не раньше.
- [ ] 5.2 Описание: первым экраном результат, а не список технологий.
- [ ] 5.3 Онбординг по образцу «три шага» с копированием шорткодов.
- [ ] 5.4 Отзывы: 0 у обоих, кодом не решается.

## Порядок и зависимости

Фаза 0 идёт первой и целиком: 0.1 и 0.2 означают, что у части пользователей плагин показывает
пустые места, 0.3 это риск снятия с каталога, 0.7 это уже нарушенный контракт с пользователями.
Фаза 1 даёт первый измеримый выигрыш и не требует API.
Фаза 4 намеренно после фазы 1: 152 шорткода, отрендеренных в iframe с noindex, не дадут ничего.

## Открытые вопросы

- Кто и когда правит api.astroway.info для фазы 3, и не сломает ли квота по домену учёт по IP.
- Нужен ли отдельный платный тир под форму посетителя: она кратно увеличивает число вызовов.
- Совместимость: снятие PUC из бесплатной сборки меняет канал обновлений у уже установленных копий.
- Что делать с очередью уже собранных версий до 0.10.x: пропускать фиксы через неё или выпускать хотфикс.
